import java.util.Scanner;
public class o04
{
	  public static void main(String...args)
	  {
		 Scanner data = new Scanner(System.in);
		 int a = data.nextInt();
		 int b = data.nextInt();
		 int c = data.nextInt();
		 boolean found = false;
		 int A = a!=0 ? 1:0;
		 int B = b!=0 ? 1:0;
		 if(A==0&&B==0&&c==0||A==0&&B==1&&c==0||A==1&&B==0&&c==0||A==1&&B==1&&c==1)
		 {
		   System.out.println("AND");
		   found = true;
		 }
		 if(A==0&&B==0&&c==0||A==0&&B==1&&c==1||A==1&&B==0&&c==1||A==1&&B==1&&c==1)
		 {
			System.out.println("OR");
			found = true;
		 }
		 if(A==0&&B==0&&c==0||A==0&&B==1&&c==1||A==1&&B==0&&c==1||A==1&&B==1&&c==0)
		 {
			System.out.println("XOR");
			found = true;
		 }
		 if(!found)
		 {
		   System.out.println("IMPOSSIBLE"); 
		 }
		 data.close();
	  }
}
